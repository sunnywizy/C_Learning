#include <stdio.h>
int main() {
    // Variable declaration
    int age;    //Declare available named 'age' of type integer.

    // Variable assignment
    age = 25;   // Assign the value 25 to the variable 'age'

    // Variable usage
    printf("I am %d years old.\n", age);    //Print the value of 'age'

    return (0);
}